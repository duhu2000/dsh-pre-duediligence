# v0.4.1 变更：尽调类型栏、提示词条默认隐藏、企业名回填修复

## 1. 输入框下方：四个尽调类型
访前尽调 / 准入尽调 / 交易尽调 / 持续尽调，icon + 文案，hover 出一句话说明。
目前只有「访前尽调」可点，其余三个灰色不可点（说明里标"即将上线"）。
挂在 DSH 的 conversation.composer.dock 插槽（输入框卡片下方一行）。

## 2. 提示词生成条默认不展示
点「访前尽调」才出现（出现时展开），再点一次隐藏；条头的箭头可收起为一行摘要。

## 3. 修复：表单里填的企业名没进输入框
原因：句子里的占位符"（这里输入企业名）"被当成用户填的企业名回收了。
现在表单里填的企业名优先，占位符本身永远不会被当成企业名；补了回归测试。

## 应用
同前：覆盖 skills/ 与 lib/，Ctrl+C 后重新 dsh web，刷新。验证 version: 0.4.1。
