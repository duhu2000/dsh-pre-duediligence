# v0.3.3 变更：产业定位独立成段 + 下载按钮不再依赖任务状态

## 1. 产业定位独立成段（回应“没有产业概览”）
- 作战卡由七段改为八段：① 核心研判 ② 产业定位 ③ 近期动态 ④ 业务假设 ⑤ 红线提示 ⑥ 现场必问 ⑦ 触达开场 ⑧ 覆盖说明。
- 产业定位不再是核心研判里的一行，而是任何档位都不得省略的独立段：主营与业务模式 / 行业分类 / 产业链环节（上下游）/ 环节特征（推理说明）。
  速览 2–3 行，标准 4 行，深度加“产业链对标（推理综述 · 非企查查事实）”。原“行业分析”段并入本段。
- 工作台右侧“固定八段作战卡”清单同步改为四字标题。

## 2. 「下载报告」按钮修复
- 问题：v0.3.2 只在“工作台里发起的任务且状态为 ready”时才渲染按钮。你的截图里工作台显示“等待任务”——
  工作台面板关闭再打开会重新挂载、任务状态丢失；直接在对话框输入的尽调也不算工作台任务。两种情况按钮都不出现。
- 修复：作战交付页始终显示按钮，只要当前 Session 里能捕获到作战卡正文就可点；无任务时面板提示“已捕获作战卡”。
- 提取器改为“最后一次工具事件之后的全部助手文本拼接”（作战卡被拆成多个文本节点也能整卡拿到），
  并移入纯模块 report-export.ts，新增 3 个单测覆盖 text / content[] / message.content 三种节点形态。

## 应用
1. 用压缩包里的 skills/ 与 lib/ 覆盖 ~/qcc-previsit-dsh/ 对应目录（两者都要）。
2. dsh web 窗口 Ctrl+C，重新 dsh web；浏览器刷新。
3. 验证（覆盖后）：
   grep "version:" ~/qcc-previsit-dsh/skills/qcc-previsit-onepager/SKILL.md   # 期望 version: 0.3.3
   grep -c "u4EA7\\\\u4E1A\\\\u5B9A\\\\u4F4D" ~/qcc-previsit-dsh/lib/client.js   # “产业定位”转义串，期望 ≥1
4. 旧会话直接打开工作台 → 作战交付 → 应能看到「下载报告」可点（旧卡是七段样式，正常）；
   重跑一单尽调看第二段“产业定位”。

## 仍需你环境验一处
若按钮仍是灰的，说明 DSH 会话节点的文本字段不在 text / content / message.content / parts 之中。
点右上角「Session log ↓」把日志发我，我对准真实字段。
