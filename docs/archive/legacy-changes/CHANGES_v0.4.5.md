# v0.4.5 变更：工作台内看报告、类型栏细节

- 尽调报告页：报告生成后直接在工作台内预览（与下载的 HTML 同一份样式），右上角「下载 HTML」；
  未生成时仍显示进度与八段结构。
- 输入框下方的尽调类型栏：圆角改为 5px，栏下方留 14px 空隙，不再贴屏幕底边。
- 「工作台」按钮只在提示词生成条展开时显示，收起时不显示。

应用同前：覆盖 skills/ 与 lib/，Ctrl+C 后重新 dsh web，刷新。验证 version: 0.4.5。
