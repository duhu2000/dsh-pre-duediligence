# 0.1.31 — 工具结果可靠性与可见进展

- 工具出口递归省略可选对象字段，拒绝JSON会静默破坏的非JSON值；工具测试在宿主边界执行严格往返校验。
- 首版 finalize / history 不再返回 undefined；相同正文重试幂等，不同正文仍不得覆盖已发布版本。遇到不确定保存结果应先读取历史确认，补充内容创建新版本。
- 新增 previsit_progress，保存用户可见的整理事实、风险核对、撰写报告工作摘要。查询事件、停止和同步失败优先；旧摘要标明时间，不展示内部思考或虚构百分比。单次长模型生成不能保证定时更新。
- 有界提取嵌套中文业务字段、记录计数和数组样本，支持JSON文本包装，排除凭据字段；查询状态与摘要缺失分开展示。老任务仅保存了旧摘要时不补造数据。
- PDF随包内置未修改的 Noto Sans CJK SC Regular 2.004（SIL OFL 1.1），默认离线导出。保留字体环境变量覆盖；只生成正文版，不声称复刻HTML排版。字体使安装包增大约16MB。

验收：类型检查、单元/集成回归、构建及发布流水线浏览器门禁。合成中文PDF已生成、提取文本并逐页检查。本机无头Chrome启动退出，浏览器回归以GitHub Linux门禁结果为准；未调用真实企业MCP或改写用户历史报告。
