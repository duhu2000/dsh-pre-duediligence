# 更新日志
## [Unreleased]

## [0.1.15] - 2026-09-11

- 取消发送任务后的企查查 MCP 额度二次确认；发送即授权所选 8 / 18 / 40 次硬上限，多候选主体仍保留人工消歧。
- 新增 Session-scoped Host 任务库，持久化主体、高层工具调用、进度、错误、报告与历史；工作台通过本地 Host API 轮询真实状态。
- 确认唯一主体后，右侧“拜访对象”立即切换为完整法律实体名称；同一 `PV-*` 任务更正主体不重置已用额度。
- 支持 Agent 实际传入的 `key_personnel` 业务维度，安全归一到 `personnel` 固定路由。
- 新增 `previsit_finalize` 八段报告完成工具及 Host HTML 下载路由；前端可将会话中已捕获的合格报告回写 Host，避免任务已完成但侧栏仍“等待开始 / 待生成”。
- 下载按钮在制品未就绪时给出明确原因，报告完成后可直接下载；任务历史同步完成、部分完成、失败或取消状态。

## [0.1.14] - 2026-09-11

- 业务入口改用 DSH 公开 `sessions.create({ workspaceId, sessionId })` 契约，将新建的命名空间 Session 直接归属当前、最近或首个可用 Workspace，不再落入“未分组”。
- 为 DSH 0.1.1 的 `workspaces.connectWorkspace` 和 0.1.2 的 `uiWorkspace.connectWorkspace` 增加普通新会话隔离；如宿主候选到访前尽调、数据清洗补全、AI 填表或招投标的业务空 Session，改用同 Workspace 中可复用的普通空 Session 或新建普通 Session。
- 普通会话复用排除已归档、跨目录、跨 Workspace 和其它业务命名空间；并发新会话请求按 Workspace 合并，失败不导航、不污染当前页。
- 无 Sidebar、Sidebar `0.18.1` 和 `0.17.1` 的隔离真实 Host 回归均通过；初始关闭、流程按钮单例 Tab、Tab X 与宿主折叠恢复行为保持不变。

## [0.1.13] - 2026-09-10

- 基础安装不再默认安装 Better Sidebar；仅在 `DSH_PREVISIT_WORKBENCH=on` 时安装成套工作台版本。
- 无 Sidebar 时保留左侧入口、专属原生会话、提示词生成/草稿回填、原生发送、bundled Skill/工具链和会话报告阅读；流程按钮给出可执行提示且不改写业务状态。
- 安装预检继续阻断当前 DSH 与已安装 Sidebar 的已知跨基线冲突，即使本次选择基础模式也不会忽略会导致宿主启动失败的现有插件。
- 连接器与产品增量安装均保留 `node-pty` 构建批准，避免已存在兼容 Sidebar 时更换本地候选包被 pnpm 的 ignored-builds 门禁中断。
- 新增 optional peer、客户端无 Sidebar 运行时导入、基础/工作台安装分支及缺失服务提示的契约测试。

## [0.1.12] - 2026-09-10

- 将候选组合收敛为 DSH `0.1.2-rc.1` / Better Sidebar `0.18.1` / MCP Connector `0.2.37`，可选 Context 共存版本为 `0.48.0`。
- 安装器在写入 Profile 前阻止新旧 DSH / Sidebar 双向混装，并阻止候选组合与未验证 Context 版本共存；Context 仍非必装依赖。
- CI 改为成对的 stable/candidate SDK 矩阵，不再测试或暗示支持跨基线笛卡尔组合。
- 记录候选组合在临时 `DSH_HOME` 的真实宿主无付费交互验收；真实企查查 Provider 与四款业务插件共装仍待验证。

## [0.1.11] - 2026-09-10

- 采纳 DSH-UX-001 v1.5.0：Better Sidebar Session 单例 Tab 成为唯一工作台容器，移除内容头关闭 X 及页脚“返回会话”容器控制。
- 流程按钮对齐对象与目标、范围确认、资料采集、证据核验和任务历史；重复定位同一视图严格 no-op，不创建任务或调用 MCP。
- 加强 0.17/0.18 capability probes、后台 Session reveal 隔离、pending reveal 与 Tab/订阅卸载清理回归。

## [0.1.10] - 2026-09-08

- 改善搜索元数据、安装说明、能力边界与四产品互链；无运行时或依赖变更。


## [0.1.9] - 2026-09-07

- 解除 Sidebar 0.17.1 精确限制，新增 0.18 公共能力适配和缺失/不兼容局部降级。
- 草稿读写绑定会话输入机；迟到发送不清空其它会话或用户新写入的草稿。
- 任务认领要求专属会话；报告仅取对应任务快照，支持 DSH text blocks，移除全页面 DOM 兜底和不完整报告下载。
- 报告生成与执行覆盖分开；区分无数据、无权限、未执行、失败和未知结果。
- 新增 Host 高层工具、真实预算/主体确认、固定 MCP 路由、ToolRuntime 防绕过、次数预算、取消和卸载清理。
- 安装器预检共同依赖，拒绝自动降级；提供 stable/candidate 基线，修正 OAuth 迁移顺序。
- 新增 Node/Sidebar/DSH SDK CI 矩阵、浏览器门禁、annotated tag / main CI 检查和 Registry/provenance 回读。
- 规范 v1.2.0 全文、真实宿主、真实 Provider 与市场验收仍待完成，详见 docs/COMPATIBILITY.md。

## [0.1.8] - 2026-09-07

- 首页输入框下方五项快捷菜单统一为数据清洗补全风格：独立描边卡片、图标在上、短标签在下。
- 卡片保持单行排列；窄屏仅在菜单容器内部横向滚动，不再切换为多行宫格，也不会撑宽页面。
- 采纳 DSH-UX-001 v1.1.3，并新增快捷菜单方向、高度、边框、单行及视口边界 UI 断言。

## [0.1.7] - 2026-09-07

- 右侧五阶段菜单统一为招投标工作台风格：等宽分栏、图标在上、短标题在下，移除容易越界的第二行描述。
- 选中阶段使用浅蓝底、蓝色文字和底部蓝色指示线；相邻阶段用细分隔线，桌面与窄屏均保持在右栏边界内。
- 采纳 DSH-UX-001 v1.1.2，并新增阶段方向、描述节点数量和横向溢出 UI 断言。

## [0.1.6] - 2026-09-07

- 修复左侧“访前尽调”入口自动展开右侧工作台：入口现在只创建并进入专属 Session，初始页保持完整对话空间。
- 右侧工作台仅由输入框下方五项业务快捷按钮显式打开；关闭后也从同一组按钮重新进入。
- 采纳 DSH-UX-001 v1.1.1 的初始开合规则，并补充入口不调用 `openTab` 的回归测试。

## [0.1.5] - 2026-09-07

- 对齐 DSH-UX-001 v1.1.0 与企查查蓝 Mockup：浅深色 Token、组件状态、键盘焦点和窄屏布局统一更新。
- 左侧菜单保持“访前尽调”，菜单、首页与工作台统一使用 Mockup 的线性建筑 LOGO；首页标题更新为“访前尽调一页纸智能体”。
- 首页改为简洁说明与输入框下方五项快捷菜单；快捷菜单只导航同一权威右侧工作台，不伪造执行进度。
- 新增四步提示词生成器：居中 Portal、固定头尾、正文滚动、Escape/Tab 焦点管理，回填不自动发送；已有草稿必须选择替换、追加或取消。
- 工作台升级为“当前任务 / 任务历史”主导航及五步业务导航，并增加关闭后的会话头恢复入口。
- 修正状态诚实性：会话停止但未捕获完整报告时保持“等待确认 / 继续”，不再误报完成；“新的尽调”不会重新认领刚清除的旧任务。
- 下载报告同步采用企查查蓝；新增规范采纳记录和 UI 契约测试。

## [0.1.4] - 2026-09-06

- 保持 sessions.create 的运行时接收者，修复读取 this.manager 导致的「打开失败，请重试」。
- 创建独立会话并校验标识，先准备目标工作台再切换会话；准备失败时保持原页面。
- 补齐中文产品首页、四阶段流程及局部可恢复标题，仅在访前尽调空白会话展示。
- 侧栏展开请求支持新会话挂载前排队，修复连续切换时右侧工作台不显示。
- 增加入口失败、会话归属、标题恢复和导航提交顺序回归测试。
