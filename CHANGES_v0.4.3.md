# v0.4.3 变更：标题、场合与尽调类型去重

- 提示词条标题改为「访前尽调提示词生成」。
- 原「目的」四项（首次拜访摸底 / 商务谈判前 / 签约·准入前核查 / 复访更新）与输入框下方的四个尽调类型有重叠：
  签约/准入前核查 = 准入尽调，复访更新 ≈ 持续尽调。
  改为「场合」三项，只保留"拜访这件事"内部的区分：首次拜访 / 谈判前拜访 / 复访。
  准入核查归「准入尽调」，无拜访的定期复查归「持续尽调」，交易对手核查归「交易尽调」。

应用同前：覆盖 skills/ 与 lib/，Ctrl+C 后重新 dsh web，刷新。验证 version: 0.4.3。
