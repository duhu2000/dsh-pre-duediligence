# v0.1.30 继续尽调与报告版本

合入远端已发布0.1.29图片/PDF企业录入和多企业计划，保留原有能力。

- 已完成报告通过独立子任务继续尽调，版本单调分配；原报告不改回运行中、不覆盖。
- 保存补充材料原文、来源日期、引用事实和交叉比对；区分同源、一致、冲突、不可比。来源声明不等于真实性已核验。
- 当前任务与历史详情可查看材料、版本及衍生文件；PDF/DOCX导出不新建尽调任务、不重新调用MCP。
- 衍生文件单独持久化，保存正文及文件哈希，支持重复导出去重。

## 使用边界

PDF需要配置有中文覆盖的 DSH_PREVISIT_PDF_FONT；Word使用打开端字体，支持 DSH_PREVISIT_DOCX_FONT 配置。当前导出保留Markdown正文标记，不承诺HTML排版复刻。文件/网页正文读取依赖宿主实际可用工具；不新增任意URL下载器。专用舆情接口未接入。

交付仅登记 awaiting-configuration 申请；真实上传适配器未配置，不会向外部发送报告，也不宣称已上传。

本地合并检查218项测试通过；PDF一页和Word两页合成样本逐页检查通过。生产DSH全链路仍需用户验收；发布由GitHub CI矩阵及隔离浏览器门禁通过后，经OIDC完成npm发布。
