/** Saved report time, never export time. Date-only legacy values retain their precision. */
export declare function formatReportTime(value?: string): string;
export declare function reportTextWithTime(markdown: string, completedAt: string): string;
