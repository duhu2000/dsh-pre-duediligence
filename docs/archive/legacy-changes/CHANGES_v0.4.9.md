# v0.4.9 变更：提示词条重新配色
全部改用 DSH 自身的中性色 token：白底面板 + 一条分隔线；标签 12px 三级灰；条件项 5px 圆角、细边框、二级灰文字，
选中态为深色实心（不再用蓝色淡底）；企业名输入框 5px 圆角；按钮无边框文字样式，只有「开始尽调」用 DSH 业务蓝。
输入框下方类型栏选中态同样改为深色实心。附预览图。
应用同前，验证 version: 0.4.9。
