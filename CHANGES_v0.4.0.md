# v0.4.0 变更：主标题、两边同一份表单

## 1. 首页主标题
"探索未至之境" → "访前尽调智能体"。实现：启动时改写 DSH 会话词典里的 hero.headline（中英文），并加 DOM 观察兜底。

## 2. 右侧工作台"尽调设定"页与输入框上方的设定条是同一份表单
- 同一个表单组件、同一份会话级状态：在任一边选条件 / 填企业名，另一边立刻一致；两边都能点「开始尽调」。
- 两边都实时写 DSH 输入框：设定条走公开动作 + DOM 兜底；工作台直接写输入框。
- 去掉"回到会话、在输入框上方设定"的提示与「去设定」按钮。

## 应用
同前：覆盖 skills/ 与 lib/，Ctrl+C 后重新 dsh web，刷新。验证 version: 0.4.0。
