# 0.1.35

- 已发送的任务在对象与目标页显示只读状态，包含模型运行但尚未创建任务记录的阶段；阻止重复开始。
- 提示词生成器明确访前技能与主体绑定路由，投资机构/深度/完整报告仍走访前流程；保留原始 MCP 防绕过保护，错误提供恢复步骤。未修改全局投资技能。
- 当前、历史 HTML 预览及下载、Word/PDF 使用保存时间，显示北京时间至秒；仅有日期的历史记录保留精度。原报告不修改，衍生文件使用新渲染缓存标识。
- 下载顺序 HTML、Word、PDF，PDF 为主按钮。

首页初始引导由总账统一规划，不属于本次变更。模型实际技能选择仍需在真实 DSH 投资机构场景回归；自动化测试不是生产宿主验收。
