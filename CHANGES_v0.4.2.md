# v0.4.2 变更：尽调类型栏在首页也显示

v0.4.1 的四个尽调类型按钮挂在 DSH 的 composer.dock 插槽，但 DSH 在首页（空白会话）不渲染这个插槽，所以看不到。
改为从设定条组件出发，把类型栏用 portal 挂到输入框卡片栈的末尾（卡片正下方），首页与会话内都显示。

应用同前：覆盖 skills/ 与 lib/，Ctrl+C 后重新 dsh web，刷新。验证 version: 0.4.2。
