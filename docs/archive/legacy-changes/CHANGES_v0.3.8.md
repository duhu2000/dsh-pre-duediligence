# v0.3.8 变更：设定条与输入框合为一体、联动修复

## 1. 一个容器，不再叠两层
参照 DSH 自己的"计划条 / 队列条"贴合方式：设定条改为输入框卡片的**顶部一段**（上圆角、无底边、底部与卡片重叠 3px、淡色底），
视觉上是同一张卡片的上半部分，而不是另一个盒子摞在上面。内部不再有二级卡片：只有"标签 + 一排条件"的行。

## 2. 联动
- 点条件 / 填企业名 → 句子实时写进下方输入框。除了走 DSH 公开的 setDraft，再加一道原生 setter + input 事件的兜底，
  30ms 后若输入框内容仍没跟上就直接写入，避免 v0.3.7 里"选中了但输入框没变"的情况。
- 输入框里手改 → 设定条实时识别为"手工内容"模式：之后点条件不覆盖，条里出现「按条件补充」追加一句。
- 「清空」同时清两边。

## 3. 展开状态
新会话默认展开；已有对话的会话默认折叠为一行摘要；发送后自动折叠。
修复 v0.3.7 在首页被裁掉一半的问题（flex 收缩导致）。

## 应用
同前：覆盖 skills/ 与 lib/，Ctrl+C 后重新 dsh web，刷新。验证 version: 0.3.8。
