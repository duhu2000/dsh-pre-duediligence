# v0.3.7 变更：尽调设定移到输入框上方

## 交互设计
DSH 提供 `conversation.input.dock` 插槽（输入框卡片正上方、同宽的一整行），本版把"尽调设定"做成这一行的「访前尽调」条：

- 折叠态（一行）：左侧「访前尽调」标识，中间是当前已选条件的小标签（企业名 · 角色 · 目的 · 关注 · 深度 · 输出），右侧箭头。点击展开/收起。
- 展开态：六行——要见谁（企业名输入框）/ 我是 / 目的 / 关注 / 深度 / 输出，每行"标签 + 一排可点的条件"。
- 核心机制：每点一个条件，句子**实时写进 DSH 原生输入框**（不再有第二个文本框）。输入框里的文字随时可以手改；
  手改过之后点选不会覆盖，条里出现「按条件补充」按钮，点它另起一句追加。
- 「开始尽调 →」走正式提交（带任务 ID），发送后条自动折叠、清空输入框、右侧工作台切到"经营研判"并开始跟踪。
- 直接按 DSH 自己的发送键也能发（不带任务 ID，工作台不跟踪进度，但报告照常生成）。
- 「重点关注」不再限制 4 项。

右侧工作台的"尽调设定"页改为只读摘要：显示本次选择的条件与已发送的任务原文，按钮「去设定」回到会话。
设定条与工作台共用同一份会话级状态（新增 previsit-store.ts）。

## 应用
同前：覆盖 skills/ 与 lib/，Ctrl+C 后重新 dsh web，刷新。验证 version: 0.3.7。
打开任意会话（包括新会话首页），输入框上方应出现「访前尽调」条。
